# Compress

## Commands for each ${BIOME}

### HEX Asset to Binary

```sh
./assets-to-bin.js "../../resources/assets/${BIOME}/tiles.data.json" "./bin" -r

./compress.js ../../resources/assets/${BIOME}$ ./${BIOME}$.tar.gz -tgz -r

```
