## auto shore

process all tiless in parallel and find all valid tiles for raw map state

next negotiatie between tiles what they have, and what matches together

when there is a place which cannot find match, see if anything matches:
  a) if not than stop following this issue
  b) if yes than try renegotiations from this point

if multiple such points are found, than try using all those starting points in parallel
